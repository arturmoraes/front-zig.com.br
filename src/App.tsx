import "./styles.css";

const InputNumber = () => {
  return <input />;
}

export default function App() {
  // const [value, setValue] = React.useState(0);

  const handleChange = () => {
    // TODO
  };

  return (
    <div className="App">
      <InputNumber />
    </div>
  );
}
